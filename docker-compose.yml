version: '3.8'

services:
  # Servidor Backend Express (com inteligência artificial Gemini, parser de planilhas/PDFs, etc.)
  crm-api:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: cbc-crm-api
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - GEMINI_API_KEY=${GEMINI_API_KEY} # Puxa da variável de ambiente configurada no Coolify ou arquivo .env
    restart: always

  # Servidor Frontend Nginx para servir arquivos estáticos e encaminhar requisições da API para o backend
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: cbc-frontend
    ports:
      - "80:80"
    depends_on:
      - crm-api
    restart: always
